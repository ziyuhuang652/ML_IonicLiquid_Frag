# Organization Report

Cleanup date: 2026-07-09

## Scope

The repository was reorganized in place under
`EMIBF4_MACE_wall_benchmark`. No files were deleted. The repository has Git
metadata, but `git ls-files` reported zero tracked files before cleanup, so the
move operation used regular filesystem moves rather than `git mv`.

## Final Layout

```text
manuscript/
data/
references/
scripts/
bash/
backup/
README.md
organization_report.md
```

## Files Moved

Manuscript and figures:

- Moved manuscript-used figures from `figures/` to `manuscript/figures/`:
  `fig1_collision_setup`, `fig1_selected_mass_spectra`,
  `fig2_fragmentation_maps`, and `fig3_product_family_comparison`, each as
  `.pdf` and `.png`.
- Moved unused generated figures to `backup/figures/`.
- Moved old manuscript planning/draft files to `backup/manuscripts/`.
- Moved LaTeX intermediates (`.aux`, `.bbl`, `.blg`, `.fdb_latexmk`, `.fls`,
  `.log`, `.out`, `.spl`, `.synctex.gz`, generated markdown fragments) to
  `backup/temp/`.
- Kept `manuscript/manuscript.tex`, `manuscript/latexmkrc`, and the compiled
  `manuscript/manuscript.pdf`.

Data:

- Retained only data required by active figures, manuscript tables, validation,
  or Figure 1 rendering.
- Moved unused processed CSVs to `backup/data/processed/`.
- Moved unused literature-derived CSVs to `backup/data/literature/`.
- Moved digitized spectra and digitization metadata to `backup/data/digitized/`.
- Moved full unused raw MACE-medium, MACE-POLAR, and ReaxFF trajectory outputs
  to `backup/data/raw/`.
- Kept one raw MACE-medium trajectory used to render the setup figure:
  `data/raw/mace_medium_42/run_E10_Y-90_Z-90/E10_Y-90_Z-90.traj`.

References:

- Moved `manuscript/references.bib` to `references/references.bib`.
- Moved DFT paper PDF and extracted source assets from `data/raw/dft_literature/`
  to `references/dft_literature/raw_dft_literature/`.
- Moved literature-review and audit notes to `references/literature_notes/`.

Scripts:

- Kept active analysis scripts in `scripts/analysis/`:
  `build_manuscript_figures.py`, `validate_release.py`, and
  `comparison_analysis.ipynb`.
- Kept active simulation drivers in `scripts/simulation/`:
  `run_mace_polar_cases.py`, `run_reaxff_wall_cases.py`, supporting input
  files, and `README.md`.
- Moved shell entry points to `bash/`:
  `run_mace_polar_42.sh`, `run_reaxff_42.sh`.
- Moved old analysis scripts and the historical MACE-medium driver to
  `backup/scripts/`.

Miscellaneous:

- Moved old layout notes, known-issues notes, manifest, and reasoning notes to
  `backup/misc/`.
- Moved Python cache directories to `backup/temp/`.

## Files Renamed

No user-facing files were renamed. Files were moved to clearer locations while
preserving their original filenames.

## Retained Data And Rationale

| File | Why retained |
| --- | --- |
| `data/processed/mass_spectra_standardized.csv` | Required for manuscript mass spectra figure. |
| `data/processed/wall_cases_normalized.csv` | Required for MACE outcome maps and validation counts. |
| `data/processed/hf_trajectory_occurrence.csv` | Required by validation and internal outcome joins. |
| `data/processed/reaxff_case_summary.csv` | Required for ReaxFF outcome maps, product comparison, and validation. |
| `data/processed/wall_products.csv` | Required for product-family comparison. |
| `data/literature/dft_energy_resolved_results.csv` | Required for DFT/MM regime strip and product-family comparison. |
| `data/benchmarks/resource_benchmark.csv` | Required for runtime benchmark validation and manuscript table. |
| `data/raw/mace_medium_42/run_E10_Y-90_Z-90/E10_Y-90_Z-90.traj` | Required to render Figure 1 collision setup. |
| `data/README.md` | Documents retained datasets and their consumers. |

## Archived Data And Rationale

- Archived full raw trajectory campaigns that are not read by the active
  manuscript plotting script.
- Archived old processed comparison tables generated by superseded figure
  scripts.
- Archived digitized spectra and metadata not used by the current manuscript
  figures after the figure-plan revision.
- Archived literature-derived HF-specific tables because the current manuscript
  no longer centers the figure story on HF formation.

## Duplicate Files Found

- BibTeX duplicate-key check: 6 entries, no duplicate keys.
- No exact duplicate-removal operation was performed.

## Paths Updated

- `manuscript/manuscript.tex`
  - `\graphicspath` updated to `figures/`.
  - bibliography path updated to `../references/references`.
- `scripts/analysis/build_manuscript_figures.py`
  - output directory updated to `manuscript/figures/`.
- `bash/run_mace_polar_42.sh`
  - repository root detection updated for top-level `bash/`.
- `bash/run_reaxff_42.sh`
  - repository root detection updated for top-level `bash/`.
- `Makefile`
  - figure target reduced to the active manuscript figure builder.
  - notebook target updated to `scripts/analysis/comparison_analysis.ipynb`.
- `scripts/simulation/README.md`
  - shell-script locations and archived known-issues path updated.
- `.gitignore`
  - updated for moved notebook output and generated LaTeX intermediates.

## Validation Results

Completed successfully:

- `python -m compileall -q scripts`
- `bash -n bash/run_mace_polar_42.sh`
- `bash -n bash/run_reaxff_42.sh`
- `make validate`
- `make figures`
- `make manuscript`

Manuscript validation:

- Entry file: `manuscript/manuscript.tex`
- Output: `manuscript/manuscript.pdf`
- Page count: 20
- PDF size after rebuild: 419156 bytes
- LaTeX warning/error scan over the generated log found no matches for:
  `Warning`, `Overfull`, `Underfull`, `undefined`, `too large`, or `Error`.

## Unresolved Issues And Manual Review

- `data/` and large trajectory formats remain ignored by `.gitignore`. This
  matches the previous public-release policy, but a separate data DOI or release
  artifact should be prepared if reviewers need the retained local data.
- `scripts/analysis/comparison_analysis.ipynb` was moved but not executed
  during cleanup, to avoid unnecessary notebook-side effects. It should be
  reviewed before relying on it as a primary reproduction artifact.
- Full raw trajectory campaigns are archived under `backup/data/raw/`; restore
  them if future manuscript revisions need trajectory-level reanalysis beyond
  the current figures.
